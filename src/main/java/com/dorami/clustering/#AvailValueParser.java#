package com.dorami.clustering;


/**
 *  Takes {@code Text} and determines the 
 */
public class CombinedMessage {

	public CombinedMessage(Text value) {
		parseValue(value);
	}

	private void parseValue(Text value) {
		String line = value.toString();
		
		// Grab the 
	}

	public int getRow() {
		
	}

	public int getColumn() {
		
	}

	public double getValue() {
		
	}
}